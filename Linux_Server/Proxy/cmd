sudo apt update

sudo apt install python3 python3-pip -y

pip3 install flask requests

python3 proxy.py

http://<your-linux-ip>:9091

sudo nano /etc/systemd/system/python-proxy.service
[
    [Unit]
    Description=Python Reverse Proxy
    After=network.target

    [Service]
    User=YOUR_USERNAME
    WorkingDirectory=/home/YOUR_USERNAME/python-proxy
    ExecStart=/usr/bin/python3 proxy.py
    Restart=always

    [Install]
    WantedBy=multi-user.target

]

sudo systemctl daemon-reload
sudo systemctl start python-proxy
sudo systemctl enable python-proxy

sudo nano /etc/systemd/system/python-proxy.service


sudo apt install python3-venv -y
python3 -m venv proxy-env
source proxy-env/bin/activate
pip install flask requests


1. Run in Background (Simple & Quick)
nohup python3 proxy.py > proxy.log 2>&1 &


Keeps running after you close the terminal

Logs go to proxy.log

Shows a process ID (PID)

Check if it's running:

ps aux | grep proxy.py


Stop it:

kill <PID>